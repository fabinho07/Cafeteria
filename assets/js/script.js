/*
function determinarSexo(nome) {
    const nomesMasculinos = "paulo"
    const nomesFemininos = "maria"
  
    if (nomesMasculinos.includes(nome)) {
      return "Masculino";
    } else if (nomesFemininos.includes(nome)) {
      return "Feminino";
    } else {
      return "Indefinido";
    }
  }
  
  
  console.log(determinarSexo("maria")); 
*/

const determinarGenero = (masculino) => {
   const masc = "homem"
   const fem = "mulher" 

   if(determinarGenero == masculino)
   return "homem"
}


console.log(determinarGenero)

 
  


















 






















 